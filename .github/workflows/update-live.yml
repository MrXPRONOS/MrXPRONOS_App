name: Mise à jour des matchs en direct (toutes les minutes)

on:
  schedule:
    - cron: '* * * * *'
  workflow_dispatch:

jobs:
  update-live:
    runs-on: ubuntu-latest
    steps:
      - name: Appeler la fonction update-live
        run: |
          curl -X POST \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            -H "Content-Type: application/json" \
            https://nhwafcpndlufzzxexikh.supabase.co/functions/v1/live/update-live